# 架构优化、清理冗余代码、修复小 bug、整理目录结构

## Context

项目从 Jinja2 SSR 迁移到 Vue 3 SPA 后（2026-07-19），大量旧 SSR 模板和页面脚本成为死代码。同时积累了几个小 bug（版本号过期、重复 MIME 定义、N+1 查询、过时注释、缺失封面资源）。本次清理旨在删除死代码、修复小 bug、整理目录结构，**不改变任何现有功能**。

版本号定为 **v2.4.2**（patch 级维护释放）。

---

## 改动清单（按风险从低到高排序）

### P1. 删除 15 个死模板 + 1 空目录
Vue 3 SPA 迁移前的旧 Jinja2 SSR 模板，无任何 Python 代码渲染它们（仅 `admin_pages.py` 使用 `TemplateResponse`，且只渲染 `admin/*.html`）。

**删除**：`templates/` 顶层 15 个 .html（base / _nav / _toast / index / login / register / music_list / diary_write / diary_detail / my_bottles / pick_bottle / mood_calendar / garden / shop / ai_chat）+ `templates/partials/` 空目录

**保留**：`templates/admin/` 整个目录（7 个文件，admin_pages.py 仍使用）+ `templates/` 根目录本身（config.py `templates_dir` 引用）

### P2. 删除 10 个死页面脚本
`static/js/pages/` 下非 admin 脚本，仅被死模板引用：ai_chat / auth / diary / diary_detail / home / mood_calendar / music / my_bottles / pick / shop

**保留**：`admin_*.js` 6 个 + `app.js`（admin 模板仍引用）

### P3. 修复 main.py 版本号
- [app/main.py:73](file:///c:/Users/Administrator/Desktop/webwrold/app/main.py#L73)：`version="1.0.0"` → `version="2.4.2"`

### P4. 删除 main.py 重复 .webp
- [app/main.py](file:///c:/Users/Administrator/Desktop/webwrold/app/main.py) EXT_TO_MIME 字典中 `.webp` 定义了两次（L173 + L184），删除 L184

### P5. 修复 pages.py 过时注释
- [app/routers/pages.py:4-5](file:///c:/Users/Administrator/Desktop/webwrold/app/routers/pages.py#L4)：注释中 `:5173` → `:5000`（Vite 端口），`:5000` → `:5001`（FastAPI 开发端口）

### P6. 修复 vite.config.js 过时注释
- [frontend/vite.config.js:6](file:///c:/Users/Administrator/Desktop/webwrold/frontend/vite.config.js#L6)：同 P5，注释 `:5173` → `:5000`，`:5000` → `:5001`

### P7. 修复 app.js 过时注释
- [static/js/app.js](file:///c:/Users/Administrator/Desktop/webwrold/static/js/app.js)：注释中 `base.html` → `admin/_base.html`（base.html 已在 P1 删除）

### P8. 新增 5 个五音封面 SVG
seed.py 引用 `cover_{yin_type}.svg` 但 `static/images/` 目录为空。新增 5 个治愈系 SVG（颜色取自 constants.py YIN_INFO）：
- `cover_gong.svg`（宫/土/健脾 #E8D5A8）
- `cover_shang.svg`（商/金/润肺 #D4D9C8）
- `cover_jue.svg`（角/木/疏肝 #A8C5A0）
- `cover_zhi.svg`（徵/火/养心 #E8B8A8）
- `cover_yu.svg`（羽/水/宁心 #A8B8C5）

### P9. 优化 admin_pages.py N+1 查询 ⚠️ 唯一中风险改动
- [app/routers/admin_pages.py:90-125](file:///c:/Users/Administrator/Desktop/webwrold/app/routers/admin_pages.py#L90)：`admin_users` 函数 for 循环内 3 个 COUNT 查询/用户 × 50 用户 = 151 次查询
- **优化为 4 次查询**：1 次查用户 + 3 个 `GROUP BY` 聚合 + 字典拼接
- 字段名、类型、返回结构完全不变，仅查询方式改变
- `user_ids` 空列表保护（`if user_ids else {}`）

### P10. 更新 6 份文档（遵循 Documentation Iron Rule）
- README.md：badge v2.4.1 → v2.4.2
- HANDOFF.md：新增 v2.4.2 🔒 提示块 + 当前阶段更新
- docs/PROJECT_STATE.md：新增 v2.4.2 节 + 状态表
- docs/ARCHITECTURE.md：新增 v2.4.2 🔒 提示块
- docs/DEPLOYMENT.md：新增 v2.4.2 🔒 提示块（无部署迁移，纯清理）
- docs/DEVELOPMENT.md：新增 v2.4.2 🔒 提示块

### P11. 前端编译 + git commit + push GitHub
- `npm run build` 重新编译 dist
- 冒烟测试（后端导入 + 服务器启动 + SPA/admin/API 验证）
- git commit + push

---

## 不动的部分（已确认仍在使用）
- `static/css/` 全部 9 个文件（admin/_base.html 加载 style.css → @import 全部模块）
- `static/js/app.js`（admin/_base.html 加载，仅 P7 改一行注释）
- `static/js/pages/admin_*.js` 6 个（admin 模板依赖）
- `static/audio/*.mp3` 5 个（seed.py 自动生成占位音频）
- `templates/admin/*.html` 7 个（admin_pages.py 渲染）
- `app/config.py` `templates_dir`（admin 模板需要）
- `app/database.py` `_migrate_legacy_columns`（无过时迁移）
- `requirements.txt`（无多余依赖）
- `app/routers/admin_pages.py` 的 `admin_user_detail`（单用户无 N+1）

---

## 验证方式
1. `python -c "from app.main import app; print(app.routes)"` — 后端导入无错
2. `npm run build` — 前端编译通过
3. 启动服务器冒烟测试：
   - `GET /` → 200（Vue SPA）
   - `GET /openapi.json` → `info.version` = `2.4.2`
   - `GET /static/images/cover_gong.svg` → 200 + `image/svg+xml`
   - `GET /admin/login` → 200（admin 模板渲染正常）
   - admin 登录后 `GET /admin/users` → 200（N+1 优化后用户列表正常）
   - `GET /api/music` → 200（cover_image 指向存在的 SVG）
   - 旧书签 `GET /mood` → 302 → `/calendar`（重定向正常）
